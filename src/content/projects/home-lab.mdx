---
title: "Home Lab"
summary: "A Proxmox home server with an isolated lab for hands-on IT experience, ethical hacking, and self-hosting."
tag: "Systems Admin"
category: "it"
cover: "../../assets/images/home-lab/homelab-cover10.svg"
coverAlt: "Home lab diagram project cover"
featured: true
featuredOrder: 2
---

import ProjectGallery from "../../components/ProjectGallery.astro";
// IMPORT IMAGES
import labTopology from "../../assets/images/home-lab/homelab-topology-diagram4.svg";
import miniPC from "../../assets/images/home-lab/minipc-merge5.png";
import proxDash from "../../assets/images/home-lab/proxmox-dash.png";
import pfsenseRules from "../../assets/images/home-lab/pfsense-fw-rules2.png";

## Project Overview

The goal was to build a home server that serves as both a learning environment and a platform for self-hosting various apps. This lab will assist in my journey as I learn more about ethical hacking, sys admin, and more.

## Lab Architecture

I wanted to create a more real-world IT environment&mdash;not simply a few virtual machines (like a Kali attacker and a victim machine).

To isolate the hacking lab from my main network, I virtualized pfSense, serving as a nested firewall/router for the lab. I also configured VLANs to segment various systems from each other.

### Network Topology

<ProjectGallery
  images={[
    {
      src: labTopology,
      alt: "Home Lab topology diagram",
    },
  ]}
/>

## Implementation

### Hardware

I installed Proxmox hypervisor on a power-efficient mini PC. To run more VMs simultaneously, I upgraded the system with a second memory chip (32GB total) and a second SSD for more storage and backups. I hope to set up network-attached storage (NAS) eventually, however.

<ProjectGallery
  images={[
    {
      src: miniPC,
      alt: "Mini PC hardware setup",
    },
  ]}
/>

### Proxmox Configuration

<ProjectGallery
  images={[
    {
      src: proxDash,
      alt: "Proxmox dashboard showing various virtual machines and system specs",
    },
  ]}
/>

### pfSense Firewall (Virtualized)

While I prefer and use OPNsense for my main network, I chose pfSense for this lab to gain experience using both.

I segmented the machines into VLANs and established firewall rules to control communication between them. I'm currently allowing the Attack VLAN access to the vulnerable device VLANs as well as the Internet:

<ProjectGallery
  images={[
    {
      src: pfsenseRules,
      alt: "pfSense firewall rules page",
    },
  ]}
/>

## Future Improvements

- Set up Wazuh to practice using SIEM (Security Information and Event Management).
- Experiment with tools like Infection Monkey to simulate/test an attacker's lateral movement within a network.
- Add network-attached storage (NAS) for an improved backup solution and self-hosted cloud storage.

**Note: This case study will improve over time as I continue to build out my home lab!**
